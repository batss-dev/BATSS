# Print the summary function for 'BATSS' outputs

Print method function for objects of class 'summary.batss'.

## Usage

``` r
# S3 method for class 'summary.batss'
print(x, ...)
```

## Arguments

- x:

  An object of class 'summary.batss' (i.e., output of the function
  [batss.glm](batss.glm.md)).

- ...:

  For future use

## See also

[`batss.glm()`](batss.glm.md), [`batss.surv()`](batss.surv.md), the
functions generating S3 objects of class 'batss'.
